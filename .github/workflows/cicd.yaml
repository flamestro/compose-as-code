name: Publish compose-as-code to npmjs
on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package: [ compose-as-code, create-composition ]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: '18.x'
          registry-url: 'https://registry.npmjs.org'

      - name: Install Dependencies ${{ matrix.package }}🗂️
        run: yarn install --frozen-lockfile
        working-directory: ./packages/${{ matrix.package }}

      - name: Test ${{ matrix.package }}🧪
        run: yarn test
        working-directory: ./packages/${{ matrix.package }}

      - name: tsc ${{ matrix.package }}🏗️
        run: yarn tsc
        working-directory: ./packages/compose-as-code

  release:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        with:
          node-version: '18.x'
          registry-url: 'https://registry.npmjs.org'

      - name: Setup Git
        run: git config --global user.email "noreplyflamestro@gmail.com" && git config --global user.name "flamestro"

      - name: Bump Version 🚀
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx lerna version patch --yes --create-release=github --conventional-commits --push --message "Release [ci skip]"

      - name: Release Packages 🚀
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx lerna publish from-package